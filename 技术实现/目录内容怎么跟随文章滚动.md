### 实践的方式

首先目录使用的是ul标签,每一条标题就是一个li标签

获取当前高亮的li标签相对于ul元素顶部的高度

如果高度小于ul高度的一半,不需要滚动

超过一半时,判断当前高亮元素的索引和上一个高亮元素的索引的之间的差值

然后根据这个差值去向上或者向下滚动(差值 * 单个li元素的高度)

方法可行,bug一堆,而且难以描述



### 新的想法,还没有实际

每次高亮li改变时,将这个li直接移动ul的中间位置就好了

去计算差值太繁琐了

后面去具体实践一下

